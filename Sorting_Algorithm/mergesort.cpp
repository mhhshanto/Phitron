#include <bits/stdc++.h>
using namespace std;

vector<int>merge_sort(vector<int>a){
  if(a.size()<=1){
    return a;
  }
  int mid = a.size()/2;
}

int main()
{
  
  
  system("pause");
  return 0;
}
